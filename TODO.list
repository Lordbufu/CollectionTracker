Global TODO List:
    - Review if i want the default user, to be able to change there own password (code already finished, just need to push/upload it).
    - Request feedback on the any extra visual validation requirments (so far feedback is positive).
    - Remove the uncommented JS section, to allow for image uploads to the barcode scanner pop-in (Done).
    - Update the readme on github, to reflect that the project is finished, and adding some meaningfull tekst. (Done)
    - Update version number to the new 'live' version, removing the beta/testing status, and updating the current version data in the footer.